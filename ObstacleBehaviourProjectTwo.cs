using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObstacleBehaviourProjectTwo : MonoBehaviour
{
    
    // private void OnCollisionEnter(Collision other) // Pasa los parametros de collision del obj
    // {
    //     if ( other.transform.tag == "Projectile") // Cuando colisione con un obj "Projectile" se destruye
    //     {
    //         Death();
    //     }
    // }

    // Update is called once per frame
    // void Death()
    // {
    //     // Destroy(gameObject,_timer); // Detruye el obj en un tiempo determinado
    //     Destroy(gameObject); // Destruye el obj cuando es colisionado por projectile
    // }
}
